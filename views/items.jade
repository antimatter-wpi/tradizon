each data in items
   div.item-box(onclick='showDetail(' + data.id + ')')
         img(src= data.photo width='200px' height='150px')
         p= data.title
         button.btn.btn-danger#delete-item(onclick="removeItem(" + data.id + ")")
         #overlay-box.overlay